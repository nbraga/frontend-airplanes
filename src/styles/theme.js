import { createTheme } from "@mui/material";

export const theme = createTheme({
  collors: {
    orange: "#ffa726",
    white: "#f1f1f1",
    dark: "#002884",
  },
  mySizes: {
    low: 10,
    med: 15,
    big: 20,
  },
});
